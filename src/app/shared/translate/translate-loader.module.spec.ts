import { TranslateLoaderModule } from './translate-loader.module';

describe('SharedModule', () => {
  /**
   *
   * Smooke tests
   *
   */
  it('should create an instance', () => {
    expect(TranslateLoaderModule).toBeTruthy();
  });
});
